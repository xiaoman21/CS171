import React from 'react';
import ReactDOM from 'react-dom/client';
import MediaDashboardSlide from './MediaDashboardSlide';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MediaDashboardSlide />
  </React.StrictMode>,
);

